# 2. Design Goals

### Primary Goals
- **Plug-and-play usage** with sensible defaults
- **Deterministic behavior** (same input → same result)
- **Offline-first** operation
- **Minimal configuration**
- **Cron compatibility**

### Non-Goals
- Cloud backup support
- GUI or TUI interface
- Block-level or filesystem-native snapshots (e.g., ZFS)
- Windows-native support without WSL
